# AI Provider Selection
AI_PROVIDER=claude-code  # Options: claude-code, azure

# Claude Code Configuration
# Uses your existing Claude premium subscription (no additional API costs)
# Install with: npm install -g @anthropic-ai/claude-code
# Authenticate with: claude (follow prompts)

# Azure OpenAI Configuration (optional - for fallback)
# Required for AI-powered daily summary generation
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=your-deployment-name
AZURE_OPENAI_API_VERSION=2024-06-01

# Application Configuration
# Cron expression for periodic update prompts (default: every 20 minutes)
PROMPT_INTERVAL_CRON=*/20 * * * *

# Cron expression for daily summary generation (default: 5 PM weekdays)
DAILY_SUMMARY_CRON=0 17 * * 1-5

# Set to 'true' to only show prompts during work hours (9 AM - 5 PM)
WORK_HOURS_ONLY=true
